# Firecrawl API 使用指南 v2.1.1

## 📋 目录

1. [概述](#概述)
2. [任务类型](#任务类型)
3. [API 端点](#api-端点)
4. [请求示例](#请求示例)
5. [响应格式](#响应格式)
6. [配置参数](#配置参数)
7. [最佳实践](#最佳实践)

---

## 概述

### v2.1.1 最新更新 (2025-11-07)

- 🆕 **URL 智能过滤**: 新增域名黑名单和首页过滤功能
- 🆕 **质量优化**: 自动过滤 Wikipedia、YouTube 等低价值域名
- 🆕 **成本节约**: 过滤发生在爬取前，节省 API 积分
- 🆕 **智能识别**: 自动区分首页和详情页，保留高质量内容
- ✅ **向后兼容**: 完全兼容 v2.1.0

### v2.1.0 主要特性 (2025-11-06)

- 🆕 **Map + Scrape 模式**: 新增 `map_scrape_website` 任务类型
- 🆕 **成本优化**: 相比传统 Crawl API 节省 **58-84%** 积分
- 🆕 **时间过滤**: 支持按发布日期范围精确爬取
- 🆕 **智能爬取**: URL 发现 + 选择性内容获取

### v2.0.0 主要特性

- ✅ **显式任务类型**: 新增 `task_type` 字段明确指定任务类型
- ✅ **配置分离**: `search_config` 和 `crawl_config` 分别管理不同类型的配置
- ✅ **向后兼容**: 兼容旧版本数据，自动判断任务类型
- ✅ **更清晰的 API**: 明确的参数和更好的文档

### 基础信息

- **Base URL**: `http://localhost:8000/api/v1`
- **认证方式**: JWT Token（开发中）
- **内容类型**: `application/json`

---

## 任务类型

### TaskType 枚举

| 类型值 | 显示名称 | 说明 | 必填字段 |
|--------|---------|------|----------|
| `search_keyword` | 关键词搜索 + 详情页爬取 | 搜索关键词，获取结果后批量爬取详情页 | `query`, `search_config` |
| `crawl_website` | 网站递归爬取 | 递归爬取整个网站的所有页面 | `crawl_url`, `crawl_config` |
| `scrape_url` | 单页面爬取 | 定期爬取单个页面内容 | `crawl_url`, `search_config` |
| `map_scrape_website` | Map + Scrape 智能爬取 | 先发现URL结构，再选择性爬取内容（支持时间过滤） | `crawl_url`, `crawl_config` |

### 任务类型对比

| 特性 | SEARCH_KEYWORD | CRAWL_WEBSITE | SCRAPE_URL | MAP_SCRAPE_WEBSITE |
|------|----------------|---------------|------------|-------------------|
| **输入** | 关键词 | 起始URL | 单个URL | 起始URL |
| **输出** | 搜索结果+详情页 | 整站内容 | 单页内容 | 筛选后的内容 |
| **API** | Search + Scrape | Crawl | Scrape | Map + Scrape |
| **数据量** | 中等（10-50页） | 大量（50-500页） | 单页 | 可控（10-100页） |
| **速度** | 中等 | 慢 | 快 | 快-中等 |
| **成本效率** | 中等 | 低（爬取全部） | 高 | **极高（按需爬取）** |
| **适用场景** | 行业资讯、竞品分析 | 网站归档、知识库 | 页面监控 | 增量更新、时间范围爬取 |

---

## API 端点

### 1. 创建搜索任务

```http
POST /api/v1/search-tasks
Content-Type: application/json
```

**请求体**:

```json
{
  "name": "string",              // 任务名称（必填）
  "description": "string",       // 任务描述（可选）
  "query": "string",             // 搜索关键词（search_keyword模式必填）
  "crawl_url": "string",         // 爬取URL（crawl_website和scrape_url模式必填）
  "task_type": "string",         // 任务类型（可选，推荐明确指定）
  "search_config": {},           // 搜索配置（search_keyword和scrape_url）
  "crawl_config": {},            // 爬取配置（crawl_website）
  "schedule_interval": "string", // 调度间隔（默认DAILY）
  "is_active": true,             // 是否启用（默认true）
  "execute_immediately": true    // 是否立即执行（默认true）
}
```

**响应**: `201 Created`

```json
{
  "id": "task_abc123",
  "name": "AI新闻监控",
  "task_type": "search_keyword",
  "task_mode": "关键词搜索 + 详情页爬取",
  "status": "active",
  "created_at": "2025-01-15T10:00:00Z",
  ...
}
```

### 2. 获取任务列表

```http
GET /api/v1/search-tasks?page=1&page_size=20
```

**查询参数**:
- `page`: 页码（默认1）
- `page_size`: 每页大小（默认20，最大100）
- `status`: 状态过滤
- `is_active`: 启用状态过滤
- `query`: 关键词模糊查询

**响应**: `200 OK`

```json
{
  "items": [...],
  "total": 50,
  "page": 1,
  "page_size": 20,
  "total_pages": 3
}
```

### 3. 获取任务详情

```http
GET /api/v1/search-tasks/{task_id}
```

**响应**: `200 OK`

```json
{
  "id": "task_abc123",
  "name": "AI新闻监控",
  "query": "人工智能 深度学习",
  "task_type": "search_keyword",
  "task_mode": "关键词搜索 + 详情页爬取",
  "search_config": {
    "limit": 10,
    "language": "zh",
    "enable_detail_scrape": true
  },
  "execution_count": 5,
  "success_count": 5,
  "success_rate": 100.0,
  ...
}
```

### 4. 更新任务

```http
PUT /api/v1/search-tasks/{task_id}
Content-Type: application/json
```

**请求体**: 所有字段可选

```json
{
  "name": "新任务名称",
  "query": "新关键词",
  "task_type": "search_keyword",
  "search_config": {...},
  "crawl_config": {...},
  "is_active": false
}
```

### 5. 修改任务状态

```http
PATCH /api/v1/search-tasks/{task_id}/status
Content-Type: application/json
```

**请求体**:

```json
{
  "is_active": false
}
```

### 6. 删除任务

```http
DELETE /api/v1/search-tasks/{task_id}
```

**响应**: `200 OK`

```json
{
  "success": true,
  "message": "任务删除成功",
  "task_id": "task_abc123",
  "task_name": "AI新闻监控"
}
```

---

## 请求示例

### 示例 1: 创建关键词搜索任务

**场景**: 定期监控 AI 领域最新资讯

```json
{
  "name": "AI新闻监控",
  "description": "监控人工智能领域的最新进展和技术动态",
  "query": "人工智能 深度学习 最新进展",
  "task_type": "search_keyword",
  "search_config": {
    "limit": 10,
    "language": "zh",
    "enable_detail_scrape": true,
    "max_concurrent_scrapes": 3,
    "scrape_delay": 1.0,
    "only_main_content": true,
    "exclude_tags": ["nav", "footer", "header", "aside"]
  },
  "schedule_interval": "DAILY",
  "is_active": true,
  "execute_immediately": true
}
```

**说明**:
- ✅ 每天执行一次搜索
- ✅ 获取 10 条搜索结果
- ✅ 自动爬取每个结果的详情页
- ✅ 最多 3 个页面并发爬取
- ✅ 只提取主要内容，排除导航等

### 示例 2: 创建网站爬取任务

**场景**: 定期归档技术博客的所有文章

```json
{
  "name": "技术博客归档",
  "description": "爬取技术博客的所有文章用于知识库建设",
  "crawl_url": "https://example.com/blog",
  "task_type": "crawl_website",
  "crawl_config": {
    "limit": 100,
    "max_depth": 3,
    "include_paths": ["/blog/*", "/articles/*"],
    "exclude_paths": ["/admin/*", "/login/*"],
    "allow_backward_links": false,
    "only_main_content": true,
    "wait_for": 1000,
    "exclude_tags": ["nav", "footer", "header"],
    "timeout": 300
  },
  "schedule_interval": "WEEKLY",
  "is_active": true,
  "execute_immediately": false
}
```

**说明**:
- ✅ 每周执行一次爬取
- ✅ 最多爬取 100 个页面
- ✅ 最大深度 3 层
- ✅ 只爬取博客和文章路径
- ✅ 排除管理页面

### 示例 3: 创建单页面爬取任务

**场景**: 定期监控官网首页内容变化

```json
{
  "name": "官网首页监控",
  "description": "每小时检查官网首页是否有更新",
  "crawl_url": "https://example.com",
  "task_type": "scrape_url",
  "search_config": {
    "only_main_content": true,
    "wait_for": 2000,
    "exclude_tags": ["nav", "footer", "header"],
    "timeout": 90
  },
  "schedule_interval": "HOURLY",
  "is_active": true,
  "execute_immediately": true
}
```

**说明**:
- ✅ 每小时执行一次
- ✅ 只爬取首页单个页面
- ✅ 等待 2 秒确保内容加载
- ✅ 创建后立即执行一次

### 示例 4: 创建 Map + Scrape 智能爬取任务（新增）

**场景**: 定期爬取最近30天的新闻内容（成本节省80%+）

```json
{
  "name": "近期新闻爬取",
  "description": "使用 Map + Scrape 模式只爬取最近30天的新闻，大幅降低成本",
  "crawl_url": "https://example.com/news",
  "task_type": "map_scrape_website",
  "crawl_config": {
    "search": "news",
    "map_limit": 100,
    "start_date": "2025-01-07T00:00:00Z",
    "end_date": "2025-02-06T00:00:00Z",
    "max_concurrent_scrapes": 5,
    "scrape_delay": 0.5,
    "only_main_content": true,
    "wait_for": 3000,
    "timeout": 90,
    "allow_partial_failure": true,
    "min_success_rate": 0.8
  },
  "schedule_interval": "DAILY",
  "is_active": true,
  "execute_immediately": true
}
```

**说明**:
- ✅ **Map API**: 快速发现所有包含 "news" 的 URL（固定 1 积分）
- ✅ **时间过滤**: 只爬取最近30天的内容
- ✅ **按需 Scrape**: 只对符合条件的页面进行爬取
- ✅ **成本优化**: 相比 Crawl API 节省 **58-84%** 积分
- ✅ **并发控制**: 5 个并发，0.5 秒延迟
- ✅ **容错机制**: 允许部分失败，最低成功率 80%

**成本对比示例**:
```
传统 Crawl API: 发现 100 个 URL，全部爬取 = 100 积分
Map + Scrape:   发现 100 个 URL，过滤后爬取 20 个 = 1 + 20 = 21 积分
节省: 79 积分 (79%)
```

### 示例 5: 更新任务配置

**场景**: 修改搜索任务的关键词和配置

```json
{
  "query": "人工智能 机器学习 ChatGPT",
  "search_config": {
    "limit": 20,
    "enable_detail_scrape": true,
    "max_concurrent_scrapes": 5
  }
}
```

**说明**:
- ✅ 只更新指定的字段
- ✅ 其他字段保持不变
- ✅ 自动重新调度

---

## 响应格式

### 任务响应对象

```json
{
  "id": "task_abc123",
  "name": "AI新闻监控",
  "description": "监控人工智能领域最新进展",
  "query": "人工智能 深度学习",
  "crawl_url": null,
  "task_type": "search_keyword",
  "task_mode": "关键词搜索 + 详情页爬取",
  "search_config": {
    "limit": 10,
    "language": "zh",
    "enable_detail_scrape": true
  },
  "crawl_config": {},
  "schedule_interval": "DAILY",
  "schedule_display": "每日",
  "schedule_description": "每天执行一次（上午8点）",
  "is_active": true,
  "status": "active",
  "created_by": "current_user",
  "created_at": "2025-01-15T10:00:00Z",
  "updated_at": "2025-01-15T10:00:00Z",
  "last_executed_at": "2025-01-16T08:00:00Z",
  "next_run_time": "2025-01-17T08:00:00Z",
  "execution_count": 5,
  "success_count": 5,
  "failure_count": 0,
  "success_rate": 100.0,
  "average_results": 8.6,
  "total_results": 43,
  "total_credits_used": 53
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 任务唯一标识符 |
| `task_type` | string | 任务类型枚举值 |
| `task_mode` | string | 任务模式描述（用于前端显示） |
| `query` | string | 搜索关键词（search_keyword模式） |
| `crawl_url` | string | 爬取URL（crawl/scrape模式） |
| `search_config` | object | 搜索配置 |
| `crawl_config` | object | 爬取配置 |
| `execution_count` | integer | 总执行次数 |
| `success_count` | integer | 成功次数 |
| `success_rate` | float | 成功率（%） |
| `average_results` | float | 平均结果数 |
| `total_results` | integer | 累计结果总数 |
| `total_credits_used` | integer | 累计消耗积分 |

---

## 配置参数

### SearchConfig (search_keyword 和 scrape_url)

```json
{
  // 搜索参数
  "limit": 10,                      // 搜索结果数量（默认10）
  "language": "zh",                 // 搜索语言（默认zh）
  "include_domains": [],            // 白名单：只爬取这些域名（可选）
  "strict_language_filter": true,   // 严格语言过滤（默认true）

  // URL 过滤参数（v2.1.1 新增）
  "exclude_domains": [              // 黑名单：排除这些域名（默认["wikipedia.org", "youtube.com", "youtu.be"]）
    "wikipedia.org",
    "youtube.com",
    "youtu.be"
  ],
  "filter_homepage": true,          // 是否过滤首页URL（默认true）

  // 详情页爬取控制
  "enable_detail_scrape": true,     // 是否爬取详情页（默认true）
  "max_concurrent_scrapes": 3,      // 最大并发数（默认3）
  "scrape_delay": 1.0,              // 爬取间隔秒数（默认1.0）

  // Scrape 选项
  "only_main_content": true,        // 只提取主要内容（默认true）
  "wait_for": 2000,                 // 等待加载毫秒（默认2000）
  "exclude_tags": ["nav", "footer"], // 排除HTML标签（默认["nav","footer","header","aside"]）
  "timeout": 90                     // 超时秒数（默认90）
}
```

**URL 过滤配置说明** (v2.1.1 新增):

1. **域名黑名单过滤** (`exclude_domains`):
   - 自动过滤包含黑名单域名的搜索结果
   - 默认排除: `wikipedia.org`, `youtube.com`, `youtu.be`
   - 支持自定义域名列表
   - 过滤发生在详情页爬取之前，节省 API 积分
   - 示例: `["wikipedia.org", "youtube.com", "example.com"]`

2. **首页 URL 过滤** (`filter_homepage`):
   - 自动识别并过滤首页 URL，只保留详情页
   - 首页特征: `/` 结尾、`/index.html`、`/home`、纯域名等
   - 智能检测: 包含日期路径、文章 ID 的 URL 会被保留
   - 默认启用，可设为 `false` 关闭
   - 提高内容质量，避免爬取低价值页面

3. **域名白名单** (`include_domains`):
   - 可选配置，指定只爬取特定域名
   - 与黑名单互斥，优先使用白名单
   - 示例: `["example.com", "blog.example.com"]`

**过滤优先级**:
```
1. 域名黑名单检查 (exclude_domains) - 最高优先级
2. 首页模式检查 (filter_homepage)
3. 详情页特征检查 - 可覆盖首页判断
```

**使用示例**:
```json
{
  "query": "人工智能 新闻",
  "search_config": {
    "limit": 20,
    "exclude_domains": [          // 自定义黑名单
      "wikipedia.org",
      "youtube.com",
      "twitter.com",
      "reddit.com"
    ],
    "filter_homepage": true,      // 过滤首页
    "enable_detail_scrape": true
  }
}
```

### CrawlConfig (crawl_website)

```json
{
  "limit": 100,                     // 最大页面数（默认100）
  "max_depth": 3,                   // 最大爬取深度（默认3）
  "include_paths": ["/blog/*"],     // 包含路径模式（默认[]）
  "exclude_paths": ["/admin/*"],    // 排除路径模式（默认[]）
  "allow_backward_links": false,    // 是否允许向后链接（默认false）
  "only_main_content": true,        // 只提取主要内容（默认true）
  "wait_for": 1000,                 // 等待加载毫秒（默认1000）
  "exclude_tags": ["nav", "footer"], // 排除HTML标签（默认["nav","footer","header"]）
  "timeout": 300,                   // 整体超时秒数（默认300）
  "poll_interval": 10               // 状态轮询间隔秒数（默认10）
}
```

### MapScrapeConfig (map_scrape_website) - 新增

```json
{
  // Map API 配置
  "search": "news",                 // URL/标题搜索关键词（可选）
  "map_limit": 5000,                // Map返回URL数量限制（默认5000，API最大值）

  // 时间过滤配置（核心优势）
  "start_date": "2025-01-01T00:00:00Z",  // 起始日期（ISO格式，可选）
  "end_date": "2025-02-01T00:00:00Z",    // 结束日期（ISO格式，可选）

  // Scrape API 配置
  "max_concurrent_scrapes": 5,      // 最大并发Scrape数（默认5）
  "scrape_delay": 0.5,              // Scrape间隔秒数（默认0.5）
  "only_main_content": true,        // 只提取主要内容（默认true）
  "wait_for": 3000,                 // 等待加载毫秒（默认3000）
  "exclude_tags": ["nav", "footer"], // 排除HTML标签（默认["nav","footer","header","aside"]）
  "timeout": 90,                    // 单个Scrape超时秒数（默认90）

  // 错误处理配置
  "allow_partial_failure": true,    // 允许部分失败（默认true）
  "min_success_rate": 0.8           // 最低成功率（默认0.8，即80%）
}
```

**Map + Scrape 配置说明**:

1. **核心优势 - 时间过滤**:
   - 设置 `start_date` 和 `end_date` 实现按时间范围爬取
   - 只对 `publishedDate` 在时间范围内的页面进行 Scrape
   - **成本节省**: 时间过滤比例越高，节省越多（最高84%）

2. **URL 过滤**:
   - `search` 参数过滤包含特定关键词的 URL 或标题
   - 例如: `"search": "blog"` 只获取包含 "blog" 的 URL

3. **并发控制**:
   - `max_concurrent_scrapes`: 控制同时进行的 Scrape 数量
   - `scrape_delay`: 每次 Scrape 之间的延迟，避免请求过快
   - 推荐配置: 3-5 并发，0.5-1.0 秒延迟

4. **容错机制**:
   - `allow_partial_failure: true`: 允许部分 Scrape 失败，任务继续
   - `min_success_rate: 0.8`: 要求至少 80% 的 Scrape 成功
   - 适用于不稳定的网站或大批量爬取

**使用场景**:
- ✅ 只需要最近 N 天的内容（增量更新）
- ✅ 定期爬取，只获取新增内容
- ✅ 关注成本效益，需要精确控制
- ✅ 网站有清晰的 URL 结构
- ❌ 首次全量爬取（建议用 crawl_website）
- ❌ 网站无发布日期信息

### ScheduleInterval 选项

| 值 | 显示名称 | 说明 | 间隔分钟数 |
|----|---------|------|-----------|
| `HOURLY` | 每小时 | 每小时执行一次（整点） | 60 |
| `DAILY` | 每日 | 每天执行一次（上午8点） | 1440 |
| `WEEKLY` | 每周 | 每周一执行一次（上午8点） | 10080 |
| `MONTHLY` | 每月 | 每月1号执行一次（上午8点） | 43200 |

**获取所有选项**:
```http
GET /api/v1/search-tasks/schedule-intervals
```

---

## 最佳实践

### 1. 任务类型选择

**关键词搜索 (search_keyword)**:
- ✅ 需要获取多个来源的信息
- ✅ 关注行业动态、竞品分析
- ✅ 需要定期更新的资讯类内容
- ❌ 不适合：特定网站的全站爬取

**网站爬取 (crawl_website)**:
- ✅ 需要归档整个网站内容
- ✅ 构建知识库或文档库
- ✅ 定期备份网站内容
- ❌ 不适合：单页监控、实时更新需求、成本敏感场景

**单页面爬取 (scrape_url)**:
- ✅ 监控特定页面变化
- ✅ 定期更新的公告、新闻页
- ✅ 快速获取单页内容
- ❌ 不适合：需要多页面数据的场景

**Map + Scrape 智能爬取 (map_scrape_website)** - 🆕 强烈推荐:
- ✅ **增量更新**: 只爬取最近 N 天的新内容
- ✅ **成本敏感**: 相比 Crawl 节省 58-84% 积分
- ✅ **精确控制**: 按时间范围、URL 模式筛选
- ✅ **定期任务**: 每日/每周爬取新增内容
- ✅ **URL 结构清晰**: 网站有良好的 URL 组织
- ❌ 不适合：首次全量爬取、网站无发布日期

### 2. 配置优化

**关键词搜索优化**:
```json
{
  "enable_detail_scrape": true,   // 启用详情页爬取
  "max_concurrent_scrapes": 3,    // 平衡速度和资源（推荐2-5）
  "scrape_delay": 1.0,            // 避免请求过快（推荐1-2秒）
  "only_main_content": true       // 减少噪音
}
```

**网站爬取优化**:
```json
{
  "limit": 50,                    // 合理的页面限制（避免过大）
  "max_depth": 2,                 // 避免爬取过深（推荐1-3）
  "exclude_paths": ["/admin/*"],  // 排除不相关路径
  "timeout": 300                  // 足够的超时时间
}
```

**Map + Scrape 优化** - 🆕:
```json
{
  "search": "blog",               // URL过滤关键词
  "map_limit": 100,               // 合理的URL发现限制
  "start_date": "最近30天",        // 时间范围（核心优势）
  "max_concurrent_scrapes": 5,    // 并发控制（推荐3-5）
  "scrape_delay": 0.5,            // 请求延迟（推荐0.5-1.0秒）
  "allow_partial_failure": true,  // 容错机制
  "min_success_rate": 0.8         // 最低成功率80%
}
```

**成本优化决策树**:
```
是否需要完整历史内容？
├─ 是 → 使用 crawl_website（首次爬取）
└─ 否 → 是否定期更新？
    ├─ 是 → 使用 map_scrape_website（增量爬取，节省80%成本）✅
    └─ 否 → 是否单页监控？
        ├─ 是 → 使用 scrape_url
        └─ 否 → 使用 search_keyword
```

### 3. 调度间隔选择

| 场景 | 推荐间隔 | 理由 |
|------|---------|------|
| 实时新闻监控 | HOURLY | 及时获取最新内容 |
| 行业资讯跟踪 | DAILY | 平衡时效性和资源消耗 |
| 知识库更新 | WEEKLY | 内容更新频率较低 |
| 月度报告归档 | MONTHLY | 按月周期更新 |

### 4. 错误处理

**常见错误及解决方案**:

1. **任务创建失败**
   - 检查必填字段是否完整
   - 验证 task_type 与配置字段是否匹配
   - 确认 URL 格式正确

2. **详情页爬取失败**
   - 降低 max_concurrent_scrapes
   - 增加 scrape_delay
   - 增加 timeout

3. **网站爬取超时**
   - 减少 limit
   - 减少 max_depth
   - 增加 timeout

### 5. 性能优化

**提高效率**:
- 使用 `only_main_content: true` 减少内容量
- 合理设置 `exclude_tags` 过滤无用内容
- 使用 `include_paths` 和 `exclude_paths` 精准爬取

**控制成本**:
- 设置合理的 `limit` 限制
- 使用 `is_active: false` 暂停不需要的任务
- 选择合适的 `schedule_interval`

---

## 完整字段参考

### 顶层字段

#### 必填字段

| 字段名 | 类型 | 说明 | 验证规则 | 示例 |
|--------|------|------|----------|------|
| `name` | string | 任务名称 | 1-100字符，必填 | `"美国科技公司新闻"` |

#### 可选字段

| 字段名 | 类型 | 默认值 | 说明 | 验证规则 | 示例 |
|--------|------|--------|------|----------|------|
| `description` | string | `null` | 任务描述 | 最多500字符 | `"抓取美国科技新闻网站"` |
| `query` | string | `null` | 搜索关键词 | 1-200字符；task_type为search_keyword时必填 | `"OpenAI"` |
| `crawl_url` | string | `null` | 爬取的URL | 最多500字符；task_type为crawl_website或scrape_url时必填 | `"https://example.com"` |
| `target_website` | string | `null` | 主要目标网站 | 最多200字符 | `"openai.com"` |
| `task_type` | string | 自动识别 | 任务类型 | 可选值：`search_keyword`、`crawl_website`、`scrape_url`、`map_scrape_website` | `"search_keyword"` |
| `schedule_interval` | string | `"DAILY"` | 调度间隔 | 固定值：`DAILY` | `"DAILY"` |
| `is_active` | boolean | `true` | 是否启用 | true/false | `true` |
| `execute_immediately` | boolean | `true` | 创建后立即执行 | true/false | `true` |

### SearchConfig 完整字段

适用于 `task_type` 为 `search_keyword` 或 `scrape_url` 的任务。

#### 基础搜索参数

| 字段名 | 类型 | 默认值 | 说明 | 验证规则 | 示例 |
|--------|------|--------|------|----------|------|
| `limit` | integer | `20` | 搜索结果数量 | 正整数 | `10` |
| `sources` | array[string] | `[]` | 搜索来源 | 字符串数组 | `["google", "bing"]` |
| `categories` | array[string] | `[]` | 搜索类别 | 字符串数组 | `["news", "tech"]` |
| `language` | string | `"zh"` | 搜索语言 | 语言代码 | `"zh"`, `"en"` |

#### 域名过滤

| 字段名 | 类型 | 默认值 | 说明 | 示例 |
|--------|------|--------|------|------|
| `include_domains` | array[string] | `[]` | 仅包含的域名 | `["openai.com", "anthropic.com"]` |
| `exclude_domains` | array[string] | `["wikipedia.org", "youtube.com", "youtu.be"]` | 排除的域名 | `["example.com"]` |
| `filter_homepage` | boolean | `true` | 是否过滤首页URL | true/false |

#### 时间范围

| 字段名 | 类型 | 默认值 | 说明 | 可选值 |
|--------|------|--------|------|--------|
| `time_range` | string | `null` | 搜索时间范围 | `"day"`, `"week"`, `"month"`, `"year"` |

#### 详情页爬取控制

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `enable_detail_scrape` | boolean | `true` | 是否爬取详情页 |
| `max_concurrent_scrapes` | integer | `3` | 最大并发数 |
| `scrape_delay` | float | `1.0` | 爬取间隔秒数 |

#### 高级选项

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `enable_ai_summary` | boolean | `false` | 启用AI摘要 |
| `extract_metadata` | boolean | `true` | 提取元数据 |
| `follow_links` | boolean | `false` | 跟踪链接 |
| `max_depth` | integer | `1` | 最大爬取深度 |

#### HTML清理选项

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `only_main_content` | boolean | `true` | 仅提取主要内容 |
| `remove_base64_images` | boolean | `false` | 移除base64图片 |
| `block_ads` | boolean | `true` | 屏蔽广告 |
| `scrape_formats` | array[string] | `["markdown", "html", "links"]` | 爬取格式 |
| `include_tags` | array[string] | `null` | 包含的HTML标签 |
| `exclude_tags` | array[string] | `null` | 排除的HTML标签 |
| `wait_for` | integer | `null` | 等待时间（毫秒） |
| `timeout` | integer | `90` | 超时秒数 |
| `strict_language_filter` | boolean | `true` | 严格语言过滤 |

### CrawlConfig 完整字段

适用于 `task_type` 为 `crawl_website` 的任务。

#### 爬取限制

| 字段名 | 类型 | 默认值 | 说明 | 验证规则 |
|--------|------|--------|------|----------|
| `limit` | integer | `100` | 最大爬取页面数 | 正整数 |
| `max_depth` | integer | `3` | 最大爬取深度 | 正整数 |

#### 路径过滤

| 字段名 | 类型 | 默认值 | 说明 | 示例 |
|--------|------|--------|------|------|
| `include_paths` | array[string] | `[]` | 包含的路径模式 | `["/blog/*", "/news/*"]` |
| `exclude_paths` | array[string] | `[]` | 排除的路径模式 | `["/admin/*", "/api/*"]` |
| `allow_backward_links` | boolean | `false` | 允许向后链接 | `true`/`false` |

#### 页面爬取选项

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `only_main_content` | boolean | `true` | 仅提取主要内容 |
| `wait_for` | integer | `1000` | 页面加载等待时间（毫秒） |
| `exclude_tags` | array[string] | `["nav", "footer", "header"]` | 排除的HTML标签 |

#### 超时设置

| 字段名 | 类型 | 默认值 | 说明 | 单位 |
|--------|------|--------|------|------|
| `timeout` | integer | `300` | 整体爬取超时 | 秒 |
| `poll_interval` | integer | `10` | 状态轮询间隔 | 秒 |

### MapScrapeConfig 完整字段

适用于 `task_type` 为 `map_scrape_website` 的任务。

#### Map API 配置

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `search` | string | `null` | URL/标题搜索关键词 |
| `map_limit` | integer | `5000` | Map返回URL数量限制 |

#### 时间过滤配置

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `start_date` | string | `null` | 起始日期（ISO格式） |
| `end_date` | string | `null` | 结束日期（ISO格式） |

#### Scrape API 配置

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `max_concurrent_scrapes` | integer | `5` | 最大并发Scrape数 |
| `scrape_delay` | float | `0.5` | Scrape间隔秒数 |
| `only_main_content` | boolean | `true` | 只提取主要内容 |
| `wait_for` | integer | `3000` | 等待加载毫秒 |
| `exclude_tags` | array[string] | `["nav", "footer", "header", "aside"]` | 排除HTML标签 |
| `timeout` | integer | `90` | 单个Scrape超时秒数 |

#### 错误处理配置

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `allow_partial_failure` | boolean | `true` | 允许部分失败 |
| `min_success_rate` | float | `0.8` | 最低成功率 |

### 字段使用注意事项

#### 1. 任务类型自动识别

系统会根据提供的字段自动识别任务类型：

- 提供 `query` → 自动识别为 `search_keyword`
- 提供 `crawl_url` + `crawl_config` → 自动识别为 `crawl_website`
- 提供 `crawl_url` + `search_config` → 自动识别为 `scrape_url`
- 提供 `crawl_url` + `task_type="map_scrape_website"` → Map+Scrape模式

#### 2. 配置字段互斥

- `search_config` 适用于 `search_keyword` 和 `scrape_url`
- `crawl_config` 适用于 `crawl_website` 和 `map_scrape_website`
- 同时提供两者时，系统根据 `task_type` 选择使用

#### 3. 语言代码支持

支持的 `language` 值：
- `zh` - 中文
- `en` - 英文
- `ja` - 日文
- `ko` - 韩文
- `es` - 西班牙文
- `fr` - 法文
- `de` - 德文

#### 4. 时间范围说明

`time_range` 字段值及含义：
- `day` - 最近一天
- `week` - 最近一周
- `month` - 最近一个月
- `year` - 最近一年
- `null` - 不限制时间

#### 5. 深度爬取说明

- `max_depth` 值说明：
  - `1` - 仅爬取起始页面
  - `2` - 爬取起始页面 + 链接页面
  - `3+` - 递归爬取更深层级
- 建议值：1-5，过深可能导致性能问题

#### 6. 域名过滤优先级

- `include_domains` 和 `exclude_domains` 同时存在时：
  - 先应用 `include_domains`（白名单）
  - 再应用 `exclude_domains`（黑名单）
- 建议：优先使用 `include_domains` 限定范围

---

## 附录

### 错误码

| 状态码 | 说明 | 处理方式 |
|--------|------|---------|
| 200 | 成功 | - |
| 201 | 创建成功 | - |
| 400 | 请求参数错误 | 检查请求体格式和必填字段 |
| 404 | 任务不存在 | 验证任务ID是否正确 |
| 500 | 服务器错误 | 查看服务器日志，联系技术支持 |

### 版本变更

#### v2.1.1 (当前版本) - 2025-11-07

- 🆕 **URL 过滤增强**: `SearchConfig` 新增 `exclude_domains` 域名黑名单
- 🆕 **首页过滤**: 新增 `filter_homepage` 配置，自动过滤首页 URL
- 🆕 **默认黑名单**: 默认排除 Wikipedia、YouTube、优酷等低价值域名
- 🆕 **智能检测**: 支持详情页特征检测，避免误过滤
- 🆕 **过滤统计**: 提供详细的过滤统计信息
- ✅ 向后兼容 v2.1.0

#### v2.1.0 - 2025-11-06

- 🆕 **新增任务类型**: `map_scrape_website` (Map + Scrape 智能爬取)
- 🆕 **新增配置**: `MapScrapeConfig` 支持时间范围过滤
- 🆕 **成本优化**: 相比传统 Crawl 节省 58-84% 积分
- 🆕 **智能过滤**: 支持按 `publishedDate` 时间范围筛选
- 🆕 **并发控制**: 可配置并发数和请求延迟
- ✅ 向后兼容 v2.0.0

#### v2.0.0

- ✅ 新增 `task_type` 字段
- ✅ 新增 `crawl_config` 配置
- ✅ 新增 `task_mode` 响应字段
- ✅ 更新请求示例
- ✅ 向后兼容 v1.x 数据

#### v1.x (旧版本)

- 隐式任务类型判断
- 统一使用 `search_config`

---

**文档版本**: v2.1.1
**最后更新**: 2025-11-07
**API 版本**: v1
